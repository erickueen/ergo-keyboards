name: Build firmware

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/checkout@v4
        with:
          repository: moergo-sc/zmk
          ref: main
          path: src

      - uses: cachix/install-nix-action@v25
        with:
          nix_path: nixpkgs=channel:nixos-22.05

      - uses: cachix/cachix-action@v14
        with:
          name: moergo-glove80-zmk-dev
          skipPush: true

      - name: Generate keymap SVGs with Nix
        run: nix-build -A keymapSvgs -o generated-keymaps

      - name: Refresh committed SVGs for auto-PR
        if: github.event_name != 'pull_request'
        run: cp -R generated-keymaps/. docs/keymaps/

      - name: Build CI bundle
        run: nix-build -A ci -o ci-build

      - name: Create SVG regeneration PR
        if: github.event_name != 'pull_request'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "docs: regenerate keymap SVGs"
          title: Regenerate keymap SVGs
          body: |
            Generated by `nix-build -A keymapSvgs` because committed SVGs differed from the Nix-generated output.
          branch: ci/regenerate-keymap-svgs
          delete-branch: true
          add-paths: docs/keymaps/*.svg

      - name: Upload all firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-all
          path: ci-build/firmware/*.uf2

      - name: Upload GO60 firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-go60
          path: ci-build/firmware/go60.uf2

      - name: Upload Glove80 firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-glove80
          path: ci-build/firmware/glove80.uf2

      - name: Upload Corne firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-corne
          path: |
            ci-build/firmware/corne_left.uf2
            ci-build/firmware/corne_right.uf2

      - name: Upload Urchin firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-urchin
          path: |
            ci-build/firmware/urchin_left.uf2
            ci-build/firmware/urchin_right.uf2

      - name: Upload settings reset firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-settings-reset
          path: ci-build/firmware/settings_reset.uf2
